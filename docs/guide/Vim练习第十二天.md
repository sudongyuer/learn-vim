# Vim练习第十二天

> 掌握多文件之间的跳转

## 处理包裹字符的符号

- 功能由`vim-surround`插件提供
- 改变包裹字符的符号
	- `cs` + 已存在的包裹字符（eg：""）+要变更的包裹字符
- 添加包裹字符
	- `ys` +文本对象+要添加的包裹字符
- 删除已存在的包裹字符
	- `ds` + 存在的包裹字符
- 可视化模式下添加包裹字符
	- `S` + 要添加的包裹字符
